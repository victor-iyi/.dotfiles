- defaults:
    link:
      relink: true

# Remove dead symbolic links in parent folder (no recursive clean - recommended)
- clean: ['~']

# Recursively remove dead symlinks in .config directory
# - clean:
#   ~/.config:
#     recursive: true

- link:
    ~/.aliases: home/aliases
    ~/.functions: home/functions
    ~/.gitconfig: home/gitconfig
    ~/.gitignore_global: home/gitignore_global
    ~/.p10k.zsh: home/p10k.zsh
    ~/.ripgreprc: home/ripgreprc
    ~/.tmux.conf: home/tmux.conf
    ~/.vimrc: home/vimrc
    ~/.zshrc: zsh/zshrc
    ~/.zshenv: zsh/zshenv
    ~/.zprofile: zsh/zprofile
    ~/.config:

# Create directories if they don't exist.
- create:
    - ~/dev/
    - ~/.vim/undodir

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  -
    command: ./scripts/homebrew.zsh
    description: Setup homebrew installation
    stdout: true
    stdin: true
